{
  "name": "sns-pubsub-manager",
  "files": [
    "lib/**/*"
  ],
  "version": "0.1.1",
  "description": "A library to manage a SNS PubSub",
  "main": "lib/index.js",
  "scripts": {
    "prepare": "rm -rf lib; npm run build",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^17.0.21",
    "prettier": "^2.5.1",
    "typescript": "^4.6.2"
  },
  "dependencies": {
    "aws-sdk": "^2.1092.0"
  }
}
